<h1>New attachable</h1>

<!-- <%= render 'form' %> -->

<%= form_for @attachable, :url => attachable_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :file %>
<% end %>

<%= link_to 'Back', attachables_path %>
